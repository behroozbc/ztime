import { ZoroastrianMonthPipe } from './zoroastrian-month-pipe';

describe('ZoroastrianMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new ZoroastrianMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
